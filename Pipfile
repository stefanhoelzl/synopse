[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
dataclasses = ">=0.6"

[dev-packages]
# testing
pytest = ">=3.8.1"

# linter
mypy = ">=0.630"
pylint = "<2.0.0"
pytest-flake8 = ">=1.0.2"
pytest-pycodestyle = ">=1.3.0"

[requires]
python_version = "3.6"
